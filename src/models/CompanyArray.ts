import Company from "./Company";
import Filter from "./FilterStrategy";
import FilterStrategy from "./FilterStrategy";
class CompanyArray{
    public list: Company[];
    private filterStrategy: FilterStrategy;
    //private sortingStrategy: SortingStrategy:
}